            /**
             * \brief Called when the element <b>${searchTerm}</b> is found.
             * \param query Const reference to the ${type} class.
             */
            virtual void visited${name}(const ${type}& query) {}
