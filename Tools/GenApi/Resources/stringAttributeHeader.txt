        /**
         * \brief Provides access to the <b>${searchTerm}</b> attribute.
         *
         * \return The <b>${searchTerm}</b> enumerated value or an empty string 
         *  the value is not found. 
         */
        const String& get${name}(const String& notFound="") const;
